define(['angular'], function (angular) {
  'use strict';

  angular.module('Utils', [])
    .factory('Utils', ['$q', '$parse', 'messages', '$window', '$log','$filter', function ($q, $parse, messages, $window, $log, $filter) {
    	
    var module = {
    	
    }
    

    return module;
  }]);
});